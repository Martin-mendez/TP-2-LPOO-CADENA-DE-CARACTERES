/* 1. Pedir al usuario que ingrese su nombre y luego imprimirlo en pantalla. */

#include <stdio.h>
#include <string.h>

int main() {
    char nombre[100];

    printf("Ingrese su nombre: ");
    scanf("%99s", nombre); 
    
    printf("Hola, %s!\n", nombre); 
    
    return 0;
}
